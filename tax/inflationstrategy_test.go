package tax_test

import (
	"testing"
)

func Test_Brackets(t *testing.T) {
	/*
		tests := []struct {
			name          string
			grossIncome   int64
			inflationRate float64
			yearOffset    int
			expected      float64
		}{
			{
				name:          "with nil input",
				grossIncome:   0,
				inflationRate: 0,
				yearOffset:    0,
			},
		}

		for _, test := range tests {
			t.Run(test.name, func(t *testing.T) {
				assert := assert.New(t)
				brackets := makeBrackets()
				totalTax := brackets.TotalTax(test.grossIncome, test.inflationRate, test.yearOffset)
				assert.Equal(test.expected, totalTax)
			})
		}
	*/
}
